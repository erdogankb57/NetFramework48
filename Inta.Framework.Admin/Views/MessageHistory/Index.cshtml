@{
    ViewBag.Title = "View";
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.ModuleName = "Tanımlamalar";
    ViewBag.PageName = "Banner Tipleri";
}

<div class="form-content">
    <div class="col-lg-12">
        <form id="search">
            <div class="form-group">
                <div class="row">
                    <div class="col-lg-2">
                        Konu
                    </div>
                    <div class="col-lg-10">
                        <input type="text" class="form-control" id="Subject" />
                    </div>
                </div>
            </div>

            <div class="form-group">
                <div class="row">
                    <div class="col-lg-2">
                        Aktif mi?
                    </div>
                    <div class="col-lg-10">
                        <input type="checkbox" id="IsActive" checked />
                        <label for="IsActive">Evet</label>
                    </div>
                </div>
            </div>

            <div class="form-group">
                <button type="button" id="filterRecord" onclick="SearchDataList()" class="btn btn-standart" data-toggle="modal">
                    Filtrele
                </button>
            </div>
        </form>
    </div>
</div>
<br />
<div class="listMessage">
</div>

<div class="form-content">


    <div class="row">
        <div class="col-lg-12">
            <div class="table-responsive">
                @{
                    List<PagingDataListColumn> col = new List<PagingDataListColumn>();
                    col.Add(new PagingDataListColumn
                    {
                        Name = "Select",
                        Text = "Seçiniz",
                        Width = 30
                    });
                    col.Add(new PagingDataListColumn
                    {
                        Name = "Id",
                        Text = "Id",
                        Width = 100,
                        Short = true

                    });
                    col.Add(new PagingDataListColumn
                    {
                        Name = "Name",
                        Text = "Ad",
                        Short = true
                    });

                    col.Add(new PagingDataListColumn
                    {
                        Name = "IsActive",
                        Text = "Durum",
                        Width = 100

                    });
                    col.Add(new PagingDataListColumn
                    {
                        Name = "Edit",
                        Text = "Düzenle",
                        Width = 100

                    });
                    col.Add(new PagingDataListColumn
                    {
                        Name = "Delete",
                        Text = "Sil",
                        Width = 100

                    });
                }
                @Html.PagingDataListFor(ObjectId: "MessageHistoryList", Header: col, Url: "/MessageHistory/GetList", AddUrl: "/MessageHistory/Add", isAddPopup: true, DeleteUrl: "/MessageHistory/Delete", ActiveUrl: "/MessageHistory/Active", PassiveUrl: "/MessageHistory/Passive", SaveUrl: "/MessageHistory/Save", CallBackFunction: "SearchDataList", PagingRowCount: 20, OrderColumn: "Id")

            </div>
        </div>
    </div>
</div>


@section scripts{
    <script type="text/javascript">
        var SearchDataList = function () {
            $(function () {
                debugger;
                var val = { Name: null, IsActive: true };
                if ($("#Subject").val() != "") {
                    val["Subject"] = $("#Subject").val();
                }
                if ($("#IsActive").val() != "") {
                    val["IsActive"] = $("#IsActive").is(":checked");
                }

                $PagingDataList.ReloadData("MessageHistoryList", val);

            })
        }
        SearchDataList();
        $("#saveForm").submit(function (e) {
            debugger;
            e.preventDefault();
            $PagingDataList.Save("saveForm", "MessageHistoryList", SearchDataList);

        });
    </script>
}
